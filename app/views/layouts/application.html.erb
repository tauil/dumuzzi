<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
	  <title>Dumuzzi</title>
	  <meta name="keywords" content="layout css3 javascript jquery html5"/>
    <meta name="description" content="Este é um layout base."/>
	  <!--<link rel="shortcut icon" href="images/favicon.ico" type="image/x-icon" />-->
    <%= stylesheet_link_tag "layout.css.scss.erb" %>
	</head>
  <body>
    <div id="container">
      <header>
        <nav>
          <ul>
            <li><a href="/"><%= I18n.t :home %></a></li>
            <li><a href="/terms"><%= I18n.t :terms %></a></li>
            <li><a href="/about"><%= I18n.t :about %></a></li>
            <% if @authentications %>
              <li><a href="/domains"><%= I18n.t :domain %></a></li>
              <li><%= image_tag("user.jpeg", :alt => (I18n.t :logged), :title => (I18n.t :logged)) %></li>
            <% else %>
              <li><a href="/auth/twitter" ><%= image_tag("twitter_32.png", :alt => (I18n.t :log), :title => (I18n.t :log)) %></a></li>
            <% end %>
          </ul>
        </nav>
      </header>
      
      <section>
        <article>
          <%= yield %>
        </article>
        
        <aside>
          <% unless @hosts.nil? %>
          <div id="sidebar">
            <h1><%= I18n.t :online %></h1>
            <ul>
              <% @hosts.each do |host| %>
                <li><a href="http://<%= host.hostname %>"><%= host.hostname %></a></li>
              <% end %>
            </ul>
          </div>
          <% end %>
          <% unless @new_hosts.nil? %>
          <div id="sidebar">
            <h1><%= I18n.t :newest %></h1>
            <ul>
              <% @new_hosts.each do |host| %>
                <li><a href="http://<%= host.hostname %>"><%= host.hostname %></a></li>
              <% end %>
            </ul>
          </div>
          <% end %>
          <% unless @testers.nil? %>
          <div id="sidebar">
            <h1><%= I18n.t :testers %></h1>
            <ul>
              <% @testers.each do |tester| %>
                <li><a href="http://<%= tester.hostname %>"><%= tester.hostname %></a></li>
              <% end %>
            </ul>
          </div>
          <% end %>
        </aside>
      </section>
      
      <footer>
        <b>Atribuido a licença Open Source Initiative OSI - Common Development and Distribution License (CDDL-1.0)</b>
      </footer>
    </div>
  </body>
</html>  
