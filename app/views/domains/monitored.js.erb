var monitored = 'domains/monitored';
var domain_id = '<%= @domain.id %>';
var domain = $("#domain_"+ domain_id +"_monitor");
domain.empty();
domain.append(''+
'<a onclick="change_state(domain_id, monitored)" href="javascript:void(0);">'+
  '<%= show_icon(@domain.monitor, "enabled", "disabled") %>'+
'</a>');
